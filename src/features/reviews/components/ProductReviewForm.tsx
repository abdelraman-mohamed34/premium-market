'use client'
import { Star } from 'lucide-react'
import { useState } from 'react'
import { useCreateReview } from '@/hooks/use-reviews'
export default function ProductReviewForm({ productId }: { productId: string }) { const mutation = useCreateReview(productId); const [rating, setRating] = useState(0); const [comment, setComment] = useState(''); return <form onSubmit={(event) => { event.preventDefault(); if (!rating) return; mutation.mutate({ productId, rating, comment }, { onSuccess: (result) => { if (result.success) { setRating(0); setComment('') } } }) }} className="space-y-4 rounded-lg border border-gray-200 bg-white p-5"><h3 className="font-semibold">أضف تقييمك</h3><div className="flex gap-1" dir="ltr">{[1, 2, 3, 4, 5].map((value) => <button type="button" key={value} aria-label={`${value} stars`} onClick={() => setRating(value)}><Star className={`h-6 w-6 ${value <= rating ? 'fill-amber-400 text-amber-400' : 'text-gray-300'}`} /></button>)}</div><textarea value={comment} onChange={(event) => setComment(event.target.value)} placeholder="اكتب تعليقك" className="min-h-24 w-full rounded-md border border-gray-300 p-3" maxLength={500} required /><button type="submit" disabled={mutation.isPending || !rating} className="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground disabled:opacity-50">{mutation.isPending ? 'جار الإرسال...' : 'إرسال التقييم'}</button></form> }
