import Link from 'next/link'
import { CheckCircle2 } from 'lucide-react'
import TenantLink from '@/shared/components/TenantLink';
export default async function OrderConfirmationPage({ params, searchParams }: { params: Promise<{ order_id: string }>; searchParams: Promise<{ number?: string }> }) { const { order_id: orderId } = await params; const { number } = await searchParams; return <main className="mx-auto flex min-h-[65vh] max-w-xl flex-col items-center justify-center px-6 text-center"><CheckCircle2 className="h-14 w-14 text-green-600" /><h1 className="mt-5 text-3xl font-bold">Order confirmed</h1><p className="mt-2 text-sm text-gray-500">Your order has been received and is now being processed.</p><div className="mt-6 w-full border border-gray-200 p-4 text-sm"><div className="flex justify-between gap-4"><span className="text-gray-500">Order number</span><strong>{number || orderId}</strong></div></div><TenantLink href="/products" className="mt-6 bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Continue shopping</TenantLink></main> }
